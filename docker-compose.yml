services:
  db:
    image: postgres:15.2-alpine
    restart: always
    env_file:
      - ".env"
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "5432"
    volumes:
      - db:/var/lib/postgresql/data

volumes:
  db:
    driver: local
